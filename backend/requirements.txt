# Waypoint API — Python Dependencies
# Phase 1: Backend skeleton, DB, auth, Cognee memory

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
supabase>=2.0.0
python-jose[cryptography]>=3.3.0

# Memory (Cognee)
cognee>=1.0.0

# LLM (raw Anthropic SDK — no LangChain/CrewAI per AGENT.md)
anthropic>=0.40.0

# HTTP client
httpx>=0.27.0

# Encryption (BYOK key encryption helper — uses pgcrypto via SQL)
# pgcrypto is a Postgres extension, no Python package needed;
# we call it via SQLAlchemy raw SQL.

# Dev/test
pytest>=8.0.0
pytest-asyncio>=0.24.0
python-dotenv>=1.0.0
